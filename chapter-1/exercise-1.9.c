//Write a program replacing each string of more than one space to a single space
#include <stdio.h>

int main(){
    char c, d;
    char prev, curr, next;
    
    while((c = getchar()) != EOF){
	if(d == ' ' && c == ' '){
	    
	}else{
	    putchar(c);
	}

	d = c;
    }
}
